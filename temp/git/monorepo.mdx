### Lerna

개발인원이 적은 환경에서 모두 풀스택으로 개발을 해야하는데 
frontend와 backend를 따로 둘 필요성을 못느꼇다.

한 피쳐에 대해서 브랜치 하나만 파서 양쪽에서 작업해주는게 깔끔하다고 생각했다.

혹여 나중에 공통으로 유지할 변수같은것도 있을 수 있다는 판단하에 monorepo를 도입하게 되었다.

모노레포는 [lerna](https://lerna.js.org/)가 유명한것같아서 채택했다.
